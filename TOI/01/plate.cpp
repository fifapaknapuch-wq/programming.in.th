#include<bits/stdc++.h>
using namespace std;
#define Fifa67king ios::sync_with_stdio(0);cin.tie(0);
#define st first
#define id second
using pii = pair<int,int>;
map<int,int> mp;
int main(){
    Fifa67king;
    int nc,ns;cin >> nc >> ns;
    vector<queue
    while(ns--){
        int u,v;cin >> u >> v;
        mp[v] = u;
    }
    while(true){
        char c;cin >> c;
        if(c == 'X'){
            cout << 0;
            return 0;
        }
        if(c == 'E'){
            int n;cin >> n;

        }
    }
}